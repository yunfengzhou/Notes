1、富文本编辑器采用UEditor，但是没有Python版本，这里采用别人封装的文件，使用过程中貌似不能输入中文。
2、七牛云：banner图、评论内容上传到这里。
3、以发布帖子为例：发布帖子需要创建一个模型（model），通过Python manage.py映射到数据库中；来到前台views中写一个URL
渲染一个模板（HTML），get请求页面，post请求发送数据，对于需要验证权限的地方，采用装饰器；post请求需要表单验证，所以forms中
新建一个表单验证，看该输入字符的地方是否输入了字符，之后可以通过db.session.add(XXX)的方式写入到数据库。
4、celery（指挥官）：task任务，broker中间人可以用redis，worker执行，backend存储执行结果，具体实现可以从官网找到答案。
5、表单提交时多采用ajax的方式，只是从底层提交用户请求，不会直接刷新页面导致结果丢失，这样用户体验友好。
6、对于帖子的排序是排序功能的实现，二对于板块的管理实际是过滤功能，一般采用。
7、获取cms个人信息，绑定g对象，用钩子函数获取全局对象g，注销的时候只要清除session中的信息，再重定向到登录前的页面，
redirect(url_for('endpoint'))。视图函数中的endpoint一般默认为函数名称，在使用类视图的过程中会单独指定endpoint，否则默认为类的小写。
8、